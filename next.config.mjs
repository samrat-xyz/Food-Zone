/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ["randomuser.me", "i.pravatar.cc", "example.com"],
  },
};

export default nextConfig;
